# Скопируйте этот файл в .env и заполните своими значениями.
# Файл .env НЕ попадает в git (см. .gitignore).

SECRET_KEY=ваша_случайная_строка_на_32_символа

DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=ваш_пароль
DB_NAME=coworking
DB_PORT=5432

# Для docker compose (порт приложения на хосте)
APP_PORT=5000

ADMIN_EMAIL=admin@yourcompany.com
ADMIN_PASSWORD=ВашНадёжныйПароль123!
ADMIN_NAME=Администратор